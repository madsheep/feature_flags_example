== Feature flags example

Example and testing ground for doing feature based development (the real features, not some crazy cucumber bullshit).

* Every feature lives inside features directory
* Every feature is a separate engine
* Use ```config/features.yml``` to select which feature is enabled in which environment
